#include <stdio.h>
#include <stdlib.h>

#include "Vegas.h"

int main(int argc, char *argv[]) {
    if (argc < 2) {
        fprintf(stderr, "usage %s number_of_iterations\n", argv[0]);
        exit(0);
    }
}
